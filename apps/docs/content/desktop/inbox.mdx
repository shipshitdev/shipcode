# Inbox

The Inbox view is a focused triage queue for threads that need human attention. Where [Mission Control](/desktop/mission-control) is a broad dashboard, the Inbox is the "what should I look at next" list.

The component is [`apps/desktop/src/renderer/components/InboxView.tsx`](https://github.com/shipshitdev/shipcode/blob/master/apps/desktop/src/renderer/components/InboxView.tsx).

## What shows up here

By default, the Inbox surfaces threads in these states:

- `awaiting_approval` — pipeline paused after review, waiting for human sign-off (only when `requireApproval` is enabled in Settings)
- `failed` — any phase errored out
- Verification-exhausted — verifier hit `maxReviewRounds` without passing

Completed and in-progress threads are deliberately filtered out — those live in Mission Control or the per-project Kanban.

## Row layout

Each row shows:

- Project name
- Issue title + number
- Current phase / state
- Time since last state change
- Primary action button (Approve, Retry, Dismiss, Open Detail)

## "Go to issue"

Clicking a row navigates you into the thread's project and opens the IssueDetail overlay — same behavior as Mission Control click-through. This is the main reason the Inbox exists as a separate surface: it's optimized for "see the problem, open it, fix it, move on" without sidebar juggling.

## Filtering

Use the filter controls at the top to narrow by:

- Project
- State (awaiting_approval / failed / verification_exhausted)
- Age

## Related

- [Mission Control](/desktop/mission-control) — broader dashboard across all activity
- [Settings → Notifications](/desktop/settings) — toggle OS + badge notifications for the same events that populate the Inbox
