# 2026-07-03

## Session 1 — Open-work triage: 3 PRs + 74 issues → Opus 4.8 handoff doc

### System flow

```text
gh pr list / gh issue list (74 issues, 3 PRs)
  → verify claims against master 5d8065a8 (grep/read spot checks)
  → subagent sweep of backlog issues #7–#251 (staleness + overlap)
  → docs/audits/02-open-work-triage.md (handoff: merge order, closures,
    stale-body edits, splits, phased implementation queue)
```

### Affected components

- Docs only: `docs/audits/02-open-work-triage.md` (new), this session log. Zero code changes.

### What was done

- [x] Reviewed PRs #315, #273, #272: all CI-green, 0 behind master.
- [x] Root-caused why #315/#272 show `BLOCKED` despite green CI: master ruleset has `required_review_thread_resolution: true`; both have unresolved minor CodeRabbit threads. #273 is `CLEAN`.
- [x] Verified the #315 thread is valid: `.agents/memory/worktrees.md` example uses `thread.branch`; real field is `worktreeBranch` (`pipeline-core.ts:74`, usage `execution-phases.ts:222`).
- [x] Verified DRY-wave issue claims on master: `_resolvePhaseModels` dup (`pipeline-scheduler.ts:348`), no `turn-started`/`turn-completed` handling in either PipelineEvent switch (`cli-emitter.ts` has no default case), kanban dead helpers in `packages/ui/src/kanban-board/utils.ts`, no `MIGRATIONS` array yet.
- [x] Swept backlog #7–#251 via subagent: staleness, overlaps, checklist-vs-code status for follow-up issues #249/#250/#251.
- [x] Wrote `docs/audits/02-open-work-triage.md` — the Opus 4.8 implementation handoff.

### Key decisions

- **Merge order #273 → #315 → #272.** Wave issues #280–#313 cite `docs/audits/01-dry-slop-audit.md`, which exists only in PR #273 — merge before implementing waves.
- **Recommend closing #75 and #249 as already done** (github_graphql tool fully shipped with tests; all 3 secret-scan hardening items live in the workflow). Recommendation only — Vincent closes.
- **#314 judged near-stale**: dashed Opus slugs are an intentional legacy-alias layer (`OPENROUTER_MODEL_ALIASES`); only a placeholder string remains, itself a valid shorthand.
- **#250 should be split**: contains 3 confirmed live bugs (v27 migration overwrites `raw_output`; `worktreeHasChanges` returns `true` on git probe failure; sidebar resize-listener leak on mid-drag unmount) buried in a 12-item grab-bag.
- **Doc placed at `docs/audits/02-*`** continuing the 00/01 audit-doc series from PRs #272/#273.

### Files changed

- `docs/audits/02-open-work-triage.md` (new)
- `.agents/SESSIONS/2026-07-03.md` (this file)

### Mistakes and fixes

- First grep for kanban helpers targeted `apps/desktop/src` — they live in `packages/ui/src/kanban-board/`; re-searched repo-wide before concluding anything was stale.
- Summarizer subagent initially returned a status line instead of its deliverable (had spawned its own sub-research); resumed it with an explicit "return the full report now" message.

### Next steps

- Vincent: resolve the 1 thread on #315 and ~6 on #272 (all 1–3 line fixes), merge all three PRs (then #316).
- Opus 4.8: execute phases per `docs/audits/02-open-work-triage.md` §5 (Wave 1 first, #280 leading). All Wave-3 decisions now recorded on the issues.
- Untracked item to fold into #278: promote the new `Build` job to a required check after first green master run post-#272.

## Session 2 — Decision interview + backlog hygiene applied

Interviewed Vincent on the owed decisions; applied everything via `gh` (no code changes).

### Decisions

- **#309**: canonical `getDefaultBranch` fallback = symbolic-ref → main → master → current → 'main'.
- **#306**: add `model_deprecated` to shared union; desktop copy "Model deprecated — pick a replacement in Settings → Models" (warning tone).
- **#311**: keep open, `shipcode:deferred`, no implementation now.
- **#312**: extract phase-model routing to shared package, consumed by desktop + CLI (after #298) — Vincent's call: fix the root cause, not doc the divergence.
- **Ruleset**: keep required thread resolution; codify agent behavior instead.

### Applied

- Closed: #75, #249 (done), #314 (stale), #159 (dup of #277), #124 (folded into #125), #250 (split).
- Created: #317 (3 confirmed bugs), #318 (IPC hardening), #319 (narrowing/plumbing), #320 (QA-carry verification).
- Edited: #125 (two-phase spike body/title); decision comments on #306/#309/#311/#312; stale-body corrections on #194/#211/#91/#93; scope clarification on #242.
- Repo `CLAUDE.md`: new **PR workflow** rule — resolve every review thread before ending a PR turn (ruleset requires it).
- Triage doc: prepended decisions log.
