# 2026-07-02

## Session 1 — DRY/slop audit (read-only, report-only)

### Flow

```text
knip + jscpd (mechanical evidence)
  → Workflow: 12 domain finders in parallel
    → 1 adversarial verifier per finding (refute-first)
      → completeness critic (found http-fetch + types-dup gaps)
        → manual verification of stalled agents-pkg domain
          → docs/audits/01-dry-slop-audit.md
```

### Affected components

- Docs only: `docs/audits/01-dry-slop-audit.md` (new). Zero code changes — audit was explicitly read-only.
- Session infra: `.agents/SESSIONS/2026-07-02.md` (this file).

### What was done

- [x] Ran `bunx knip` (deadcode script) + `bunx jscpd` (81 exact clones / 1,453 lines / 1.29%) as seed evidence
- [x] Multi-agent Workflow: 12 domain finders → per-finding adversarial verify → critic; ~100 subagents total across 3 resumed runs (two session-limit interruptions overnight)
- [x] agents-pkg finder stalled 3× under rate limits → verified its seeds manually inline
- [x] 54 findings confirmed/adjusted, 7 refuted, 6 explicit non-findings documented
- [x] Wrote evidence-backed report with 3-wave ROI roadmap + per-refactor risk/test table

### Key decisions

- **Per-finding adversarial verification, refute-first.** Materially corrected ~40% of findings (line numbers, counts, reversed complexity mapping in pipeline-pkg-4, refuted 7 outright). Worth the token cost — report is high-precision.
- **Refuted claims documented in report §8** so future sweeps don't re-litigate (yaml dep, e2e workspace deps, runtime re-export shims, env-var sprawl all refuted with wiring evidence).
- **Workflow resume caveat learned:** editing a workflow script prompt mid-run invalidates the cache prefix — everything after the first edited agent() call re-runs. Salvaged run-2 results from the task output file and launched a fresh part-2 workflow for missing domains instead of resuming a third time.
- **Workflow args caveat learned:** `args` passed to the Workflow tool arrived as a string, so `args.root` interpolated as `undefined` in prompts. Agents coped via cwd. Hardcode paths in scripts when it matters.

### Top findings (full detail in docs/audits/01-dry-slop-audit.md)

1. DB migration order hand-duplicated 3-4× (`db/index.ts`, `test-helpers.ts`, `schema.test.ts`) — highest-ROI fix: `MIGRATIONS` array in schema.ts.
2. Largest clone: gh-cli.ts `getPullRequestFeedback`/`getPullRequestDetail` ~112L GraphQL+parsing dup.
3. Real bug: `OpenRouterAuthStatus` declared twice incompatibly; `model_deprecated` silently collapses to `'unreachable'` in desktop UI (CLI onboard shows it correctly).
4. Both PipelineEvent switches (cli-emitter, pipeline-bridge) already silently drop `pipeline:turn-started`/`turn-completed` — no exhaustiveness guards.
5. `pipeline-scheduler._resolvePhaseModels` is a line-for-line copy of the exported `resolveIssuePhaseModels` in ipc/helpers.ts.

### Files changed

- `docs/audits/01-dry-slop-audit.md` (new, report)
- `.agents/SESSIONS/2026-07-02.md` (new, this entry)

### Mistakes and fixes

- First workflow run passed knip/jscpd paths via `args` → arrived as string → `undefined` in prompts. Fix: inlined paths into the part-2 script.
- Resumed the same run after editing prompts → prefix-cache invalidation re-ran (and re-billed) most verify agents into a session limit. Fix: salvage results from output file, fresh smaller workflow for the gap domains.

### Next steps

- Wave 1 of the roadmap (15 mechanical S-effort items) is PR-able immediately; start with the `MIGRATIONS` registry.
- Product decisions needed before Wave 3: CLI retry parity (report item 32), CLI label-only routing (item 33), default-branch fallback order (item 30).
- Optional: fix knip config-loading errors so `bun run deadcode` stops emitting ~400 lines of false-positive noise.
- Open question: `*.callbacks.test.tsx` / `*.coverage.test.tsx` families not conclusively assessed for coverage-padding.

## Session 2 — AGENTS.md under Codex 32 KiB limit (priority frontmatter + frontmatter stripping)

### Problem

Regenerated repo `AGENTS.md` hit ~54 KB — past Codex CLI's 32 KiB hard truncation limit, silently dropping the tail rules. Global memory contributed ~15.7 KB, repo memory ~38 KB.

### What was done

- [x] `scripts/sync-agent-memory.sh`: strip YAML frontmatter from inlined bodies (metadata stays in source files; Codex doesn't need `name`/`type`/`last_verified`).
- [x] `scripts/sync-agent-memory.sh`: new `priority: low` frontmatter field — low-priority files are not inlined; they get a one-line description + path pointer under a "Read on demand" subsection.
- [x] Marked `priority: low`: `agents.md` (Claude Code sub-agents — not used by Codex), `e2e-ci.md` (CI wiring detail; pointer says "MUST read before editing .github/"), `skills.md` (dir layout; also gained proper frontmatter).
- [x] Merged `worktree-defaults.md` into `worktrees.md` (they restated the same path-as-truth rule + cleanup pattern); deleted `worktree-defaults.md`; updated `MEMORY.md` pointers.
- [x] Condensed `interactive-cli-run-modes.md` 5.7 KB → ~3.4 KB — all rules, defaults, fail-closed srt semantics, and grep anchors retained; billing narrative and secondary anchors trimmed.
- [x] Condensed `claude-cli.md` "Why" narrative; fixed stale cross-ref (`feedback_clamp_ipc_error_messages.md` → `ipc-errors.md`); fixed `overview.md` cross-ref (`feedback_path_as_truth_worktrees.md` → `worktrees.md`).

### Result

- `<repo>/AGENTS.md`: 54,394 → **28,063 bytes** (under the 28,672 soft limit, well under 32 KiB hard limit).
- `~/.codex/AGENTS.md` (global-only): 15,745 → **13,369 bytes**.
- Verified idempotent (identical md5 across two runs); `bash -n` clean; no stale `worktree-defaults` refs remain.

### Key decisions

- **Priority pointer over deletion:** durable "why" archives (`e2e-ci.md` incident detail) stay intact on disk; only their inlining changed. Codex reads the pointed file on demand.
- **Frontmatter stripping at generation time** rather than removing metadata from sources — `last_verified` review workflow stays intact.
- **Global memory files untouched** — the ~13.4 KB global section is shared across repos; repo-side levers were sufficient.
- Shellcheck SC2088 (tilde-in-quotes) warnings in the script are intentional: those tildes are display strings for pointers, not paths.

### Files changed

- `scripts/sync-agent-memory.sh` — frontmatter strip + priority mechanism
- `.agents/memory/`: `MEMORY.md`, `agents.md`, `claude-cli.md`, `e2e-ci.md`, `interactive-cli-run-modes.md`, `overview.md`, `skills.md`, `worktrees.md` (merged), `worktree-defaults.md` (deleted)
- `AGENTS.md` — regenerated (also picks up the 2026-07-02 global memory updates: Fable→Opus 4.8 sunset, Codex plugin routing, two-account setup)

### Follow-up (same session, after review)

Demoted two global files to `priority: low` (edits in `~/.agents/memory/`, outside this repo): `genfeed_console_project.md` (project-specific — dead weight in every other repo's AGENTS.md; also gained proper YAML frontmatter, replacing inline metadata) and `plan_agents_use_opus.md` (Claude Code `Agent()` routing — Codex spawns no Claude subagents). Result: repo AGENTS.md 28,063 → **25,275 bytes** (~3.4 KB headroom); `~/.codex/AGENTS.md` 13,369 → **10,581 bytes**.

### Next steps

- Consider making the size check a hard failure (exit 1) once trusted.

## Session 3 — Repo-map audit + 8-point cleanup response

Two-phase session. Phase 1: full factual repo map written to
`docs/audits/00-repo-map.md` (audit session 00) — built via 6 parallel mapping
subagents (root tooling, desktop, cli, web/docs, support packages, CI/e2e) plus
inline gap-fill for pipeline/agents and the integration surface after two
mappers died on rate limits. Phase 2: Vincent replied point-by-point to the
8 headline risks; this session answered the questions and fixed everything he
directed.

### System flow

```text
Audit (docs/audits/00-repo-map.md)
   │  8 headline risks → Vincent's 8 replies
   ▼
1 TG token: question → answered (chat-notification-service.ts pipeline pings; plaintext-at-rest risk stands)
2 Two UI libs: CORRECTED — @shipshitdev/ui = shared primitives, packages/ui = app feature layer (wrappers, intentional)
3 Migration chain: question → explained (63-step hand-rolled chain + orphaned issue_edges.sql)
4 /source imports: "clean it up" → FIXED (root imports everywhere, ./source exports removed)
5 Unsigned mac app: accepted for now ("green app")
6 Build in CI: "like genfeed.ai" → FIXED (Build job added to ci.yml)
7 Toolchain drift: "fix it" → FIXED (Bun pin, knip pin, engines, coverage floor)
8 Stale docs: "fix it" → FIXED (CLI README, Opus 4.6 refs)
   │
   ▼
Verify: lint 933 files ✓ · typecheck 14 turbo tasks ✓ · verify-affected ✓ ·
        desktop main-process tests 125/125 ✓ · renderer flakes = load-induced (pass idle) ✓
```

### What was done

- [x] `docs/audits/00-repo-map.md` — 7-section factual map (system, packages,
      runtime/deploy, data/auth/integration, CI/tooling, 13 risks, 7 follow-up
      sessions), every claim file-path-evidenced; then updated with Vincent's
      corrections/decisions.
- [x] **Workspace import unification (risk 4).** All `@shipcode/{agents,db,shared}/source`
      deep imports (19 src + 8 test files) converted to package-root imports;
      `./source` export subpaths deleted from the three package.jsons. Kills the
      dual-module-instance hazard: desktop main bundles all `@shipcode/*`
      (externals are natives only), so mixed root+/source imports bundled BOTH
      dist and src copies of the same package (broken `instanceof`, duplicated
      singletons). Deliberate subpaths kept: `@shipcode/shared/worktree-path`,
      `@shipcode/ui/second-ticker`, and the ui Vite dev alias (renderer HMR).
- [x] **CI Build job (risk 6).** `ci.yml` gains `Build` (needs trust+lint+typecheck),
      mirroring genfeed.ai: PR → `turbo run build --affected`, push → full; desktop
      excluded from the sweep (its `build` script runs electron-builder) and
      compiled via `build:code` after a `--filter='@shipcode/desktop^...'` dep build.
- [x] **Toolchain drift (risk 7).** publish.yml Bun 1.3.13 → 1.3.14 (+lockstep
      comment); publish-cli Node 24 documented as deliberate (npm Trusted
      Publishing needs npm ≥ 11.5); knip pinned as root devDependency 6.23.0;
      `engines.node >=22.5.0` added; ci.yml coverage floor 80/80 → 95/90 to match
      the release contract (`scripts/coverage-summary.mjs` defaults,
      `docs/coverage-leftovers.md` snapshot passes at 95.91/91.52).
- [x] **Stale docs (risk 8).** `apps/cli/README.md` command table 5 → 14 commands;
      "PLAN (Opus 4.6)" in root README + docs phase tables (`apps/docs/content/index.mdx`,
      `pipeline/overview.mdx`) → "configured model" / "Claude (Sonnet 4.6 default)"
      per `PINNED_MODEL_DEFAULTS` (`packages/shared/src/model-catalog.ts:105`).

### Key decisions

- **Root imports + bundler alias over source-everywhere.** Converting /source →
  root touches 27 files; the reverse (all-source) would touch hundreds and break
  e2e's node-resolution of package dists. Desktop keeps its ui-only source alias
  (deliberate, for HMR); every package now resolves exactly one way per bundle.
- **Node 24 in publish-cli kept.** Not drift — npm OIDC/provenance publishing
  needs npm ≥ 11.5 (Node 22 ships npm 10). Documented inline instead of "fixed".
- **Coverage floor raised, not lowered.** CI's 80/80 was the stale value; the
  95/90 release contract is documented and the last snapshot passes it.
- **Branch protection untouched.** New Build job must go green once before being
  added to required checks (repo-settings change, Vincent's call).
- **UI libraries are layered, not duplicated** (Vincent correction): published
  `@shipshitdev/ui` = cross-project primitives; `packages/ui` = ShipCode feature
  components. Audit report corrected; follow-up session 01 descoped accordingly.
- **Unsigned macOS + notify-only updater accepted** for the pre-release phase.

### Files changed

- `docs/audits/00-repo-map.md` (new + corrections)
- 27 source/test files: `/source` → root imports (`apps/cli`, `apps/desktop/src/main`,
  `packages/pipeline`, `packages/db/src/queries/task-graphs.ts`)
- `packages/{shared,agents,db}/package.json` — `./source` exports removed
- `apps/cli/src/context.test.ts`, `apps/cli/src/commands/run.test.ts`,
  `apps/desktop/src/main/index.test.ts` — duplicate `vi.mock`/`vi.doMock`
  blocks merged (sed conversion had collided the root and /source mocks)
- `.github/workflows/ci.yml` — Build job; coverage floor 95/90
- `.github/workflows/publish.yml` — Bun 1.3.14; Node 24 comment
- `package.json` — knip 6.23.0 devDependency, `engines.node >=22.5.0`
- `apps/cli/README.md`, `README.md`, `apps/docs/content/index.mdx`,
  `apps/docs/content/pipeline/overview.mdx` — doc fixes

### Mistakes and fixes

- **Blind sed created duplicate mock specifiers.** Converting
  `vi.mock('@shipcode/db/source')` → `vi.mock('@shipcode/db')` in files that
  already mocked the root produced two mocks of the same module (last one wins →
  `getDatabase` undefined). Caught by tests in 3 files; merged blocks. Lesson:
  after mechanical import rewrites, scan test files for duplicate mock specifiers.
- **Parallel local test runs poisoned each other.** Running the desktop suite
  while another suite/typecheck ran produced rotating renderer-test failures
  (25–148s per file, different set each run). All 10 "failures" pass on an idle
  machine; main-process tests (actual blast radius) pass deterministically.
  This machine (Mac14,7) is not the Mac Studio — full gates belong to CI anyway.
- **First audit workflow burst rate-limited.** 8 parallel mappers at once →
  server-side 429 for all; chunked to 3-concurrent with per-agent retry on resume.

### Next steps

- [ ] Add `Build` to required branch-protection checks after first green run on master (Vincent).
- [ ] Audit session 02 (security): move Telegram/Discord tokens to Electron
      `safeStorage`; IPC input-validation sampling; srt sandbox failure modes.
- [ ] Audit session 03 (data layer): migration chain consolidation; fold
      `issue_edges` into the numbered chain; delete or execute the orphaned
      `20260423_issue_edges.sql`.
- [ ] Remaining follow-up sessions per `docs/audits/00-repo-map.md` §7.
