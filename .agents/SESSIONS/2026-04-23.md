# Sessions - 2026-04-23

## Session 1 - Setup Affordances, Loading States, Badge Cleanup, and Clarification State

**Status:** Complete (targeted tests/typechecks green)

### Context

This session started from several live UI affordance issues found while using the desktop app:

- the detected setup profile chip looked clickable but did nothing
- async submit buttons, including `Re-detect`, did not consistently disable or show stable-width loading state
- recent activity rows could look clickable even when no navigation target existed
- running-agent/model badges were too verbose (`Codex / GPT-5.4`)
- revision badges used `REV` text where the number alone was enough
- after answering a planner clarification and resuming planning, Issue Detail still showed the open questionnaire instead of only the submitted answers

The final state now separates pending clarification from answered clarification in persisted thread state instead of relying on renderer-only filtering.

### System Flow - Clarification Answering

```text
thread.status = clarifying
thread.clarificationRequest = pending request
          |
          v
User answers in Issue Detail
          |
          v
pipeline:answer-clarification
          |
          v
ThreadQueries.resolveClarification(...)
          |
          +--> clears pending clarification_request / clarification_answers
          |
          +--> stores answered_clarification snapshot
          |
          v
pipeline.startPlanGeneration(...)
          |
          v
Issue Detail renders read-only submitted answers
instead of the pending questionnaire
```

### What Was Done

- [x] Made detected project profiles clickable in the Setup tab
- [x] Added profile-level `suggestedContract` data so clicking a profile applies its setup commands, verify commands, env files, and related settings
- [x] Wired the Setup `Re-detect` button to the real fetching state
- [x] Added shared `LoadingButtonContent` in `packages/ui`
- [x] Applied stable-width spinner/loading content to submit and mutation buttons across the desktop renderer
- [x] Fixed `OverviewView` recent activity rows so non-navigable rows no longer show clickable affordances
- [x] Removed provider prefixes from active model badges, so badges show `GPT-5.4 - high`-style model/effort text instead of `Codex / GPT-5.4`
- [x] Replaced revision chip text from `Rev 0` / `Rev 2/3` to numeric-only `0` / `2/3`
- [x] Added explicit revision tooltips such as `Configured revisions: 0` and `Current revision: 2 of 3`
- [x] Changed clarification UI so the editable question form only renders while `thread.status === 'clarifying'`
- [x] Added an explicit persisted `answeredClarification` snapshot to `Thread`
- [x] Added DB migration `V33` with `threads.answered_clarification`
- [x] Changed `pipeline:answer-clarification` to clear pending clarification immediately and store the answered snapshot
- [x] Changed planning-phase cleanup to clear pending clarification without deleting the answered snapshot
- [x] Rebuilt `packages/shared` and `packages/db` so downstream packages that consume built declarations see the new APIs

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| Use an explicit `answeredClarification` snapshot | Pending questions and submitted answers are different states; overloading `clarificationRequest` made the UI ambiguous |
| Clear pending clarification immediately on answer submit | The renderer should not have to infer that a non-clarifying thread with `clarificationRequest` is no longer pending |
| Keep the answered snapshot after planning resumes | Users should be able to see what they submitted while the resumed planning pass is running |
| Keep model badges provider-free in compact card surfaces | The model and effort are the useful signal; provider adds width and repeats implied routing detail |
| Put revision meaning in the tooltip | The chip can stay compact while still explaining what the number represents |
| Use one shared loading-content helper | Prevents spinner insertion from resizing buttons differently across surfaces |

### Files Changed

Primary implementation files:

- `apps/desktop/src/main/ipc/register-pipeline-handlers.ts`
- `apps/desktop/src/renderer/components/IssueDetail.test.tsx`
- `apps/desktop/src/renderer/components/OverviewView.tsx`
- `apps/desktop/src/renderer/components/OverviewView.test.tsx`
- `apps/desktop/src/renderer/components/ProjectSettingsModal.tsx`
- `apps/desktop/src/renderer/components/issue-detail/IssueDetailActions.tsx`
- `apps/desktop/src/renderer/components/project-settings-modal/ProjectSettingsSetupTab.tsx`
- `packages/agents/src/project-setup.ts`
- `packages/db/src/index.ts`
- `packages/db/src/queries/threads.ts`
- `packages/db/src/schema.ts`
- `packages/pipeline/src/pipeline/planning-phases.ts`
- `packages/shared/src/schemas.ts`
- `packages/shared/src/types.ts`
- `packages/ui/src/ActivePipelineCard.tsx`
- `packages/ui/src/LoadingButtonContent.tsx`
- `packages/ui/src/kanban-board/IssueCardParts.tsx`
- `packages/ui/src/kanban-board/IssueListView.tsx`
- `packages/ui/src/kanban-board/types.ts`
- `packages/ui/src/kanban-board/utils.ts`

Test/scaffold updates:

- `apps/desktop/src/main/chat-notification-service.test.ts`
- `apps/desktop/src/main/ipc/register-github-handlers.test.ts`
- `apps/desktop/src/main/ipc/register-pipeline-handlers.test.ts`
- `apps/desktop/src/main/ipc/retry-phase.test.ts`
- `apps/desktop/src/main/notification-service.test.ts`
- `apps/desktop/src/main/pipeline-bridge.test.ts`
- `apps/desktop/src/renderer/components/ProjectSettingsModal.test.tsx`
- `apps/desktop/src/renderer/components/ThreadPanel.undo.test.tsx`
- `apps/desktop/src/renderer/components/issue-detail/PipelineTab.test.tsx`
- `apps/desktop/src/renderer/components/project-settings-modal/ProjectSettingsLeafTabs.test.tsx`
- `packages/agents/src/project-setup.test.ts`
- `packages/shared/src/model-resolution.test.ts`
- `packages/ui/src/component-regression.test.tsx`
- `packages/ui/src/kanban-board/IssuePrLinks.test.tsx`
- `packages/ui/src/kanban-board/utils.test.ts`

### Mistakes and Fixes

- **Mistake:** The first clarification fix was renderer-only: hide the pending form once the thread was no longer `clarifying`.
  - **Fix:** Added persisted `answeredClarification` state and changed the IPC handler to clear pending clarification immediately.

- **Mistake:** Typecheck initially still saw stale `Thread` and `ThreadQueries` shapes.
  - **Fix:** Rebuilt `packages/shared` and `packages/db`; downstream packages consume those built declarations.

- **Mistake:** The active model badge test still searched for the old provider-prefixed label.
  - **Fix:** Updated test expectations to match model-only badges and verify the new tooltip text.

### Verification

Project setup:

- `rtk bun run --cwd packages/agents test -- src/project-setup.test.ts`

Setup/profile/loading/overview UI:

- `rtk bun run --cwd packages/ui test -- src/component-regression.test.tsx`
- `rtk bun run --cwd packages/ui typecheck`
- `rtk bun run --cwd apps/desktop test -- src/renderer/components/ProjectSettingsModal.test.tsx src/renderer/components/project-settings-modal/ProjectSettingsLeafTabs.test.tsx src/renderer/components/OverviewView.test.tsx`

Badge cleanup:

- `rtk bun run --cwd packages/ui test -- src/component-regression.test.tsx src/kanban-board/utils.test.ts src/kanban-board/IssuePrLinks.test.tsx`
- `rtk bun run --cwd packages/ui typecheck`

Clarification state:

- `rtk bun run --cwd packages/shared build`
- `rtk bun run --cwd packages/db build`
- `rtk bun run --cwd packages/shared typecheck`
- `rtk bun run --cwd packages/db typecheck`
- `rtk bun run --cwd packages/pipeline typecheck`
- `rtk bun run --cwd apps/desktop test -- src/main/ipc/register-pipeline-handlers.test.ts src/renderer/components/IssueDetail.test.tsx`

### Known Caveats

- `apps/desktop typecheck` still reports unrelated existing export drift around `ContextGeneratorCli` and `ContextFileInfo` in the project-settings/context-generator surface.
- The repo worktree was already heavily dirty before this session; do not treat all modified files as belonging to this session.
- `@shipcode/shared` and `@shipcode/db` are consumed through built package declarations by other packages, so source-only changes in those packages may require a package build before downstream typecheck.

### Next Steps

- [ ] Fix the unrelated `ContextGeneratorCli` / `ContextFileInfo` export drift if a clean repo-wide desktop typecheck is needed
- [ ] Manually QA in the running app:
  - Setup profile chip applies suggested commands
  - `Re-detect` disables and shows spinner
  - async submit buttons keep stable size while loading
  - running card model badge is provider-free
  - revision chip tooltip explains the number
  - clarification answer submit swaps the questionnaire for the submitted-answer summary

---

## Session 3 — Approval vs Execution-Slot UX Cleanup

**Status:** Complete for the targeted UX cleanup. Targeted tests, `packages/ui` typecheck, shared build, and Biome are green. `apps/desktop` typecheck still fails on an unrelated `@shipcode/agents` export issue.

### Context

This session cleaned up the remaining confusion around the internal `awaiting_approval` phase.

The backend intentionally keeps approved threads in `awaiting_approval` while they wait for an execution slot, but several renderer surfaces still presented that as if a human approval was missing. The goal was to make every user-facing surface distinguish:

- **Needs approval**: human action still required
- **Waiting For Execution / Waiting for slot**: approval is already done; runtime is only waiting for capacity

### System Flow

```text
thread.status = awaiting_approval
          |
          v
latest plan status?
          |
   +------+------+
   |             |
   v             v
awaiting_approval   approved
   |             |
   v             v
Needs approval       Waiting For Execution
human gate           execution-capacity wait
```

### What Was Done

- [x] Added `latestPlanStatusByThreadId` to `ThreadPanelData` and used it to identify approved slot-waiters in the board.
- [x] Split approved slot-waiters into **Agent Loop → Waiting For Execution** instead of leaving them under human approval.
- [x] Excluded approved slot-waiters from `Needs approval` filtering and pending approval counts.
- [x] Added runtime-capacity copy to Project Settings so execution-slot limits are visible from the project workflow screen.
- [x] Normalized approval copy across Inbox, OS notifications, chat notifications, settings toggles, Issue Detail plan history, and the shared pipeline status step.
- [x] Removed the redundant Inbox `Approval required` badge in favor of one `Needs approval` state label.
- [x] Fixed Terminal drawer labels:
  - human approval wait now reads `Needs approval`
  - approved slot-waiters now read `Waiting for execution slot`
  - the header chip now reads `Waiting for slot`
- [x] Fixed Command Palette actions so approved slot-waiters no longer show `Approve Plan` / `Reject Plan`; they show disabled `Waiting for execution slot` instead.
- [x] Fixed Mission Control running cards so approved slot-waiters render `Waiting for slot`.
- [x] Rebuilt `@shipcode/shared` so downstream desktop typecheck sees `ActivePipelineSummary.approvedAwaitingExecution`.
- [x] Updated docs for Inbox, Kanban, Mission Control, Settings, Terminal, and Pipeline Overview.

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| Keep the backend phase machine unchanged | Queue-drain and restart behavior already rely on `awaiting_approval` + latest approved plan status; a state-machine rewrite would be broader and riskier |
| Use latest plan status to disambiguate UI | `thread.status` alone cannot tell “needs approval” apart from “already approved, waiting for capacity” |
| Keep `queued` reserved for pre-agent queueing | Post-approval slot wait is a different user-facing state and needs different messaging |
| Hide approval actions in Command Palette after approval | Showing approve/reject after approval is materially wrong, not just unclear |

### Files Changed

- `packages/shared/src/types.ts`
- `packages/shared/dist/types.d.ts`
- `packages/shared/dist/types.js`
- `apps/desktop/src/main/ipc/register-project-handlers.ts`
- `apps/desktop/src/main/ipc/register-pipeline-handlers.ts`
- `apps/desktop/src/main/notification-service.ts`
- `apps/desktop/src/main/chat-notification-service.ts`
- `apps/desktop/src/renderer/components/ThreadPanel.tsx`
- `apps/desktop/src/renderer/components/InboxView.tsx`
- `apps/desktop/src/renderer/components/IssueDetail.tsx`
- `apps/desktop/src/renderer/components/CommandPalette.tsx`
- `apps/desktop/src/renderer/components/TerminalDrawer.tsx`
- `apps/desktop/src/renderer/components/OverviewView.tsx`
- `apps/desktop/src/renderer/components/terminal-drawer/constants.ts`
- `apps/desktop/src/renderer/components/terminal-drawer/useTerminalDrawer.ts`
- `apps/desktop/src/renderer/components/terminal-drawer/TerminalDrawerHeader.tsx`
- `apps/desktop/src/renderer/components/issue-detail/helpers.ts`
- `apps/desktop/src/renderer/components/settings-panel/NotificationsSettingsSection.tsx`
- `apps/desktop/src/renderer/components/settings-panel/IntegrationsSettingsSection.tsx`
- `apps/desktop/src/renderer/components/project-settings-modal/ProjectSettingsPipelineTab.tsx`
- `packages/db/src/queries/dashboard.ts`
- `packages/ui/src/KanbanBoard.tsx`
- `packages/ui/src/ActivePipelineCard.tsx`
- `packages/ui/src/PipelineStatus.tsx`
- `packages/ui/src/kanban-board/BoardColumns.tsx`
- `packages/ui/src/kanban-board/IssueCardParts.tsx`
- `packages/ui/src/kanban-board/IssueListView.tsx`
- `packages/ui/src/kanban-board/constants.ts`
- `packages/ui/src/kanban-board/types.ts`
- `packages/ui/src/kanban-board/utils.ts`
- `apps/docs/content/desktop/inbox.mdx`
- `apps/docs/content/desktop/kanban.mdx`
- `apps/docs/content/desktop/mission-control.mdx`
- `apps/docs/content/desktop/settings.mdx`
- `apps/docs/content/desktop/terminal.mdx`
- `apps/docs/content/pipeline/overview.mdx`

### Verification

Board / filters / counts:

- `rtk bunx vitest run packages/db/src/queries/dashboard.test.ts`
- `rtk bunx vitest run packages/ui/src/kanban-board/utils.test.ts packages/ui/src/kanban-board/IssuePrLinks.test.tsx`
- `rtk bun run --cwd packages/ui test`
- `rtk bun run --cwd packages/ui test -- src/root-components.test.tsx`

Notifications / issue detail / copy:

- `rtk bun run --cwd apps/desktop test -- src/main/notification-service.test.ts src/main/chat-notification-service.test.ts src/renderer/components/InboxView.test.tsx src/renderer/components/NotificationToaster.test.tsx src/renderer/components/IssueDetail.test.tsx src/renderer/components/issue-detail/helpers.test.ts`

Terminal / command palette / overview / pipeline summaries:

- `rtk bun run --cwd apps/desktop test -- src/renderer/components/CommandPalette.test.tsx src/renderer/components/TerminalDrawer.test.tsx src/renderer/components/terminal-drawer/TerminalDrawerHeader.test.tsx src/renderer/components/OverviewView.test.tsx src/main/ipc/register-pipeline-handlers.test.ts`
- `rtk bun run --cwd packages/ui test -- src/component-regression.test.tsx`

Build / hygiene:

- `rtk bun run --cwd packages/shared build`
- `rtk bun run --cwd packages/ui typecheck`
- `rtk bunx biome check` on touched files

### Open Failure

`apps/desktop` typecheck still fails outside this session’s approval-slot work:

- Command: `rtk bun run --cwd apps/desktop typecheck`
- File: `apps/desktop/src/main/ipc/register-support-handlers.ts`
- Missing exports from `@shipcode/agents`:
  - `generateContextFiles`
  - `listContextFiles`
  - `readContextFile`

The temporary `ActivePipelineSummary.approvedAwaitingExecution` type errors were resolved by rebuilding `@shipcode/shared`.

### Next Steps

- [ ] Fix or restore the missing `@shipcode/agents` context-file exports used by `register-support-handlers.ts`
- [ ] Rerun `rtk bun run --cwd apps/desktop typecheck`
- [ ] Do live Electron QA on:
  - board approved slot-waiters
  - Command Palette while approved-but-waiting
  - Terminal drawer empty-output state
  - Mission Control running cards
