# 2026-07-13

## Session 1 — Docs: WORKFLOW.md prompt-override page

### System flow

```text
verify against source (workflow-loader.ts, workflow-prompt.ts,
  template-renderer.ts, workflow-watcher.ts, workflow-watch-manager.ts,
  planning-phases.ts / execution-phases.ts call sites)
  → new apps/docs/content/workflow.mdx
  → nav entry in content/_meta.tsx (after Configuration)
  → cross-links from configuration.mdx + desktop/skills.mdx
```

### Affected components

- `apps/docs` only. No product code changed.

### What was done

- [x] New docs page [workflow.mdx](../../apps/docs/content/workflow.mdx): file resolution (`.shipcode/WORKFLOW.md` → root `WORKFLOW.md`), front-matter agent policy table (`max_concurrent_agents`, `max_concurrent_agents_by_state`, `max_turns`, `max_retry_backoff_ms`, `execute_orchestration`, `fan_out_worker_count`, `fan_out_judge_model`, top-level `continuation_prompt`), strict Liquid template body, per-phase `TemplateContext` availability table, loud render-error semantics, live reload (30s TTL + fs.watch + `workflow:reloaded` IPC), and a comparison table vs Skills Editor / setup.json / model-override chain.
- [x] Added `workflow: 'WORKFLOW.md'` to `content/_meta.tsx`.
- [x] Linked from `configuration.mdx` (new short section) and `desktop/skills.mdx` (Related).

### Key facts verified against source (not assumed)

- Body is the **first-priority prompt for all five phases** (plan/review/revision/execute/verify); skills are the fallback only when the body is empty.
- Render errors fail the phase with `WORKFLOW.md template render error: ...` — no silent fallback (all 5 call sites).
- Per-phase extras actually passed: plan → none; review/revision/execute → `plan`; verify → `plan`, `diff`, `acceptanceCriteria`, `testOutput`. `TemplateContext.review` exists in the type but is never provided, so it was **not documented**.
- `continuation_prompt` uses simple substitution (`prior_failure_reason`, `turn_count`), not the Liquid engine.
- Watcher: valid edits commit to cache immediately; invalid edits preserve last-known-good; in-flight runs keep their start-time snapshot.

### Files changed

- `apps/docs/content/workflow.mdx` (new)
- `apps/docs/content/_meta.tsx`
- `apps/docs/content/configuration.mdx`
- `apps/docs/content/desktop/skills.mdx`

### Next steps

- PR CI validates the docs build (no local build per machine rule).
- Issue #362 (docs model-catalog page) remains separate/open.
