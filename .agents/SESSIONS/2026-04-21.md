# Sessions — 2026-04-21

## Session 1 — Side-by-Side Diff Viewer Hardening, Rollout, and Baseline Test Repair

**Status:** Complete (uncommitted)

### Context

The new full-page side-by-side diff viewer and IssueDetail Diff tab were already present in the working tree at session start. This session hardened that work: direct parser/viewer tests were added, the same viewer was rolled out to the remaining diff surfaces, file-tree accessibility was fixed, and a stale unrelated `PipelineStatus` test was repaired when the full `packages/ui` suite exposed it during verification.

There were many unrelated modified files in the worktree before this session. The changes below are only the viewer hardening/rollout slice plus the one verification-driven test repair.

### System Flow — Shared Diff Surface

```text
Execution/PR diff records exist
         |
         v
IssueDetail Diff tab / PR detail / Pipeline tab
         |
         v
  SideBySideDiffViewer
         |
         +--> file tree button click -> active file changes
         |
         +--> parse unified diff hunks/lines
         |
         +--> render old/new columns side-by-side
         |
         +--> fullscreen modal only in IssueDetail Diff tab
```

### What Was Done

- [x] Added direct regression coverage for `SideBySideDiffViewer`, including hunk parsing, paired remove/add rows, file switching, empty diff-content fallback, and empty-view fallback
- [x] Added focused `DiffTab` tests covering the no-diff state and fullscreen modal open/close behavior
- [x] Replaced the remaining old `DiffViewer` usage in PR detail with `SideBySideDiffViewer`
- [x] Replaced the remaining old `DiffViewer` usage in the pipeline sidebar with `SideBySideDiffViewer`
- [x] Removed now-unused per-panel active-diff selection state from those two desktop panels
- [x] Added explicit `aria-label`/`title` to side-by-side file tree buttons so accessible names are clean file paths instead of glyph-prefixed labels like `~src/foo.ts`
- [x] Updated desktop/UI tests to assert against the new file-button semantics
- [x] Ran focused desktop/UI tests and package typechecks
- [x] Ran full `apps/desktop` and full `packages/ui` test suites
- [x] Fixed the stale `PipelineStatus` test to reflect the current canonical phase order (`planning` -> `clarifying` -> `reviewing`)
- [x] Added a typed `IS_REACT_ACT_ENVIRONMENT` flag in the root UI regression test so the full `packages/ui` suite runs cleanly

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| Test `parseSideBySideLines()` indirectly through `SideBySideDiffViewer` | Keeps coverage on actual rendered behavior without extracting internals only for tests |
| Reuse a fixed `h-[500px]` wrapper in PR detail and PipelineTab | `SideBySideDiffViewer` expects a bounded height; this preserves the old panel footprint without redesigning surrounding layouts |
| Set file-button `aria-label` to the raw file path | Removes decorative action glyphs from the accessible name while preserving the visible icon |
| Fix `root-components.test.tsx`, not `PipelineStatus` | Shared types and component order already treat `clarifying` as a real phase between planning and review; the failure was a stale assertion |

### Files Changed

- `packages/ui/src/SideBySideDiffViewer.tsx`
- `packages/ui/src/component-regression.test.tsx`
- `packages/ui/src/root-components.test.tsx`
- `apps/desktop/src/renderer/components/issue-detail/DiffTab.test.tsx`
- `apps/desktop/src/renderer/components/pull-requests/PullRequestDetailPanel.tsx`
- `apps/desktop/src/renderer/components/pull-requests/PullRequestDetailPanel.test.tsx`
- `apps/desktop/src/renderer/components/issue-detail/PipelineTab.tsx`
- `apps/desktop/src/renderer/components/issue-detail/PipelineTab.test.tsx`

### Mistakes and Fixes

- **Mistake:** Initial Vitest reruns were started from the repo root, so they missed package-local configs and desktop setup. That produced false failures such as missing `window.shipcode` and `toBeInTheDocument`.
  - **Fix:** Re-ran tests under the package-local `vitest.config.ts` files in `apps/desktop` and `packages/ui`.

- **Mistake:** Full `packages/ui` verification surfaced a stale `PipelineStatus` expectation that still assumed the second enabled phase click was `reviewing`.
  - **Fix:** Updated the test to locate the visible `Plan`, `Clarify`, and `Review` buttons directly and assert the actual phase order.

### Verification

- `cd apps/desktop && bun x vitest run --config vitest.config.ts` -> `52` files, `238` tests passed
- `cd packages/ui && bun x vitest run --config vitest.config.ts` -> `7` files, `46` tests passed
- `bun x tsc --noEmit -p apps/desktop/tsconfig.json`
- `bun x tsc --noEmit -p packages/ui/tsconfig.json`

### Next Steps

- [ ] Manual desktop QA for the diff surfaces:
  - IssueDetail Diff tab inline viewer
  - IssueDetail fullscreen diff modal
  - Pull Request detail panel diff
  - Pipeline sidebar diff
  - File switching, scroll behavior, and narrow-width layout
- [ ] Commit this session's changes carefully; the worktree contains many unrelated pre-existing modifications, so do not `git add .`

---

## Session 2 — Global Claude Code Commands Cleanup

**Status:** Complete

### Context

Audited and cleaned up `~/.claude/commands/` — 26 global slash commands accumulated over time, many stale with wrong stack references (NestJS, MongoDB, npm/pnpm, Cursor IDE paths). Commands are lazy-loaded (zero tokens until invoked) so no performance issue, but wrong patterns actively mislead agents.

Also reviewed `.claude/agents/`, `.claude/commands/`, `.claude/hooks/` directories for repo-level usage — concluded current repo setup (CRG hooks + permission allowlist) is sufficient.

### What Was Done

- [x] Audited all 26 global commands — read each file, assessed quality/relevance/overlap
- [x] Deleted 17 commands total:
  - Stale/wrong stack: `api-test`, `docs-generate`, `migrate`, `db-setup` (all MongoDB/NestJS)
  - Superseded: `new-session` (by `session-start` skill), `quick-fix` (by session files), `docs-update` (1094 lines, NestJS/BullMQ throughout)
  - Wrong categories: `bug`, `task`, `test`, `validate` (old project structure refs)
  - Redundant: `launch`, `monitoring-setup`, `mvp-plan`, `new-cmd`, `loop`, `README.md`
- [x] Moved `/sentry` to `~/www/genfeedai/genfeed.ai/.claude/commands/sentry.md` (project-scoped)
- [x] Rewrote 6 commands: `clean`, `env-setup`, `inbox`, `optimize-prompt`, `performance`, `security-audit`
  - All now stack-agnostic with discovery-first approach
  - MongoDB/NestJS/npm refs replaced with PostgreSQL/RDS/Electron/SQLite/bun
  - `/inbox` now project-scoped at `.agents/inbox.md`, expands to GitHub issues via `gh`

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| Delete rather than revise heavily stale commands | Wrong stack throughout — full rewrites needed anyway |
| Move `/sentry` to genfeed.ai repo | Hardcoded org slug — correctly project-scoped |
| Stack-agnostic with discovery step in all revised commands | Works across shipcode (Electron/SQLite) and genfeed.ai (Next.js/RDS) |

### Files Changed

17 deleted, 1 moved, 6 rewritten in `~/.claude/commands/`.

### Final Command Inventory (8 global)

| Command | Size | Status |
|---------|------|--------|
| `/check-domain` | 7.2K | Kept |
| `/clean` | 1.5K | Rewritten |
| `/co-founder` | 14.8K | Kept |
| `/env-setup` | 1.9K | Rewritten |
| `/inbox` | 1.2K | Rewritten |
| `/optimize-prompt` | 3.7K | Rewritten |
| `/performance` | 2.8K | Rewritten |
| `/security-audit` | 3.5K | Rewritten |

---

## Session 3 — Skills Overhaul Waves 3+4 Completion (Continued)

**Status:** Complete (uncommitted)

### Context

Continuation of a previous session that implemented the 4-wave skills overhaul plan (inspired by addyosmani/agent-skills). Waves 1-2 and most of Waves 3-4 were implemented in the prior session. This session picked up at the final verification step with 2 failing shipping tests in `packages/pipeline/src/pipeline.test.ts`.

### System Flow — PR Generation Skill Resolution

```text
startShipping(threadId)
         |
         v
  deps.plans.getLatest(threadId)
         |
         v
  deps.reviews.getByPlanId(plan.id)   deps.verifications.getLatest(threadId)
         |                                       |
         v                                       v
  buildPRBody(plan, reviews, verification, issueNumber, {projectId, skills})
         |
         v
  resolveSkill('pr-generation', projectId, {skills})
         |
         +--> project override exists? use it
         +--> else: bundled default from defaults.generated.ts
         |
         v
  interpolateSkill(template, slots)
         |
         v
  append review log + verification status (outside template)
         |
         v
  gh pr create --body <body>
```

### What Was Done

- [x] Diagnosed 2 shipping test failures: `startShipping` now calls `deps.plans.getLatest`, `deps.reviews.getByPlanId`, and `deps.verifications.getLatest` — test mocks missing these methods
- [x] Added `getByPlanId: vi.fn(() => null)` to `reviews` mock in `pipeline.test.ts`
- [x] Added `getLatest: vi.fn(() => null)` to `verifications` mock in `pipeline.test.ts`
- [x] Verified all test suites pass: pipeline 78/78, agents 320/320, DB 210/210

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| Mock new deps as returning `null` | Shipping tests exercise PR creation flow, not review/verification content — null returns trigger the fallback paths correctly |

### Files Changed

- `packages/pipeline/src/pipeline.test.ts` — added missing mock methods for `reviews.getByPlanId` and `verifications.getLatest`

### Waves 3+4 Summary (across both sessions)

**Wave 3 — CLI Commands:**
- `apps/cli/src/context.ts` — shared CLI context factory
- `apps/cli/src/commands/plan.ts` — plan generation command
- `apps/cli/src/commands/approve.ts` — plan approval command
- `apps/cli/src/commands/review.ts` — review-only command
- `apps/cli/src/commands/retry.ts` — resume from checkpoint
- `apps/cli/src/commands/logs.ts` — terminal event viewer
- `apps/cli/src/commands/prd.ts` — PRD generation/enhancement
- `apps/cli/src/commands/run.ts` — refactored to use context factory
- `apps/cli/src/index.ts` — registers all 10 commands

**Wave 4 — PR Generation as PhaseSkillKey:**
- `packages/shared/src/skills-types.ts` — added `'pr-generation'` to PHASE_SKILL_KEYS
- `skills/pr-generation/SKILL.md` — new skill template with 4 slots
- `packages/agents/src/prompts/shipping-prompt.ts` — `buildPRBody` now uses `resolveSkill` + `interpolateSkill`
- `packages/pipeline/src/pipeline/execution-phases.ts` — shipping phase feeds plan/review/verification data to `buildPRBody`
- `packages/agents/src/skills/defaults.generated.ts` — regenerated (6 phases)

### Verification

- `bun run build:skills` — 6 phases ✅
- `bun run --cwd packages/pipeline test` — 78/78 ✅
- `bun run --cwd packages/agents test` — 320/320 ✅
- `bun run --cwd packages/db test` — 210/210 ✅

### Next Steps

- [ ] Commit all 4 waves as one or more commits (worktree has many unrelated pre-existing changes — stage carefully)
- [ ] Manual CLI testing: `shipcode plan <issue>`, `shipcode approve <issue>`, `shipcode logs <issue>`
- [ ] Manual pipeline run through shipping to verify PR body uses skill template
- [ ] Wave 2 supplementary skills (TDD, debugging, context engineering) need manual pipeline validation

