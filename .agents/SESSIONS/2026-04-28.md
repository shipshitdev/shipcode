# Session 2026-04-28

## Session 1: 500-user audit fixes + console UX decoupling

### Context

Continued from a 2026-04-27 audit prompted by a "25 signs your VIBE CODED app will BREAK at 500 users" thread. After classifying the points (most apply to SaaS/cloud, not desktop), three concrete fixes landed plus a follow-up UX issue Vincent raised mid-session about the console being clipped by the issue-detail panel and resetting on detail close.

### System Flow

```
Cmd+Q ──► before-quit (preventDefault)
            │
            ├─► confirmQuitForActivePipelines() (if active)
            │
            ▼
       killAllAndWait(5000)
            │
       ┌────┴───────────┐
       │                │
   pty.kill()       wait for exit events
   (SIGHUP all)         │
                        ▼
                graceMs (5s) elapsed?
                    yes ─► pty.kill('SIGKILL') for holdouts
                           wait 1s cap
                        ▼
                   app.exit(0)


selectIssue(issue) ──► terminalThreadId = issue.threadId
selectIssue(null)  ──► terminalThreadId = (preserved)        ◄── new
```

```
App layout (after)
┌─────────────────────────────────────────┐
│ Titlebar / banners                      │
├──────┬──────────────────────────────────┤
│      │ Upper region                     │
│ Side │   ┌─────────┬──────────────────┐ │
│ bar  │   │  views  │ IssueDetail (R)  │ │
│      │   └─────────┴──────────────────┘ │
│      ├──────────────────────────────────┤
│      │ TerminalDrawer (full content w)  │  ◄── no longer clipped
└──────┴──────────────────────────────────┘
```

### Affected Components

- **Backend (main process)**: `apps/desktop/src/main/index.ts` before-quit handler
- **Agents package**: `packages/agents/src/process-manager.ts` registry hygiene + new awaitable kill
- **DB package**: `packages/db/src/schema.ts` + `index.ts` migration V37
- **Renderer**: `apps/desktop/src/renderer/App.tsx` layout, `stores/app-store.ts` selectIssue semantics

### What Was Done

- [x] **P1 — ProcessManager registry leak.** Auto-delete entries via `queueMicrotask` after the `exit` event emits (and on failed-spawn path) so synchronous listeners still observe terminal state but the registry stops pinning forever. 4 new tests in `process-manager.test.ts` (drops on exit, no leak across 25 cycles, drops failed-spawns, sync visibility).
- [x] **P2 — Missing FK indexes (migration V37).** Added 4 indexes for unindexed FK columns that were causing full-scans on cascade deletes: `verifications.plan_id`, `github_issue_cache.thread_id`, `notifications.project_id`, `pipeline_checkpoints.project_id`. 3 new tests + version bump to 37.
- [x] **P3 — before-quit doesn't await pty kills.** Added `ProcessManager.killAllAndWait(graceMs)` with SIGHUP→SIGKILL escalation. Rewrote `before-quit` to always `event.preventDefault()`, await kill, then `app.exit(0)`. Added `killInFlight` guard so the second pass is no-op. 3 new tests using fake timers for SIGKILL escalation.
- [x] **GH issues filed**: shipshitdev/shipcode #62 (ProcessManager leak), #63 (FK indexes), #64 (before-quit pty orphans).
- [x] **UX — console decoupled from detail panel.** `selectIssue(null)` now preserves `terminalThreadId` so closing the right detail panel no longer clears the console. Switching to a new issue still re-pins. New test: "keeps the terminal pinned to the prior thread when the detail panel closes".
- [x] **UX — console spans full content width.** Restructured `App.tsx`: `TerminalDrawer` is now a sibling of the upper region (views + `OverlayPanel` for `IssueDetail`). `OverlayPanel`'s `absolute inset-y-0` no longer overlaps the terminal. Cursor-style full-width terminal below the detail panel.

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| `queueMicrotask` for registry deletion, not synchronous | Sync listeners on `exit` may still want to read `manager.get(id)` to inspect `exitCode`/`state`. Defer one tick → both work. |
| `killAllAndWait` with SIGHUP first, SIGKILL after 5s | claude/codex want a chance to flush; but a wedged pty must not block app shutdown. 5s + 1s post-SIGKILL cap matches typical Electron quit budgets. |
| Always `event.preventDefault()` in before-quit, then `app.exit(0)` | `app.quit()` re-triggers before-quit; `app.exit(0)` is synchronous and bypasses the handler. `killInFlight` guard handles edge case where prior code path called `app.quit()` again. |
| Skip auto-cleanup of worktrees on quit | Worktrees are intentionally thread-scoped per `.agents/memory/worktrees.md`. Auto-cleanup would orphan threads. |
| Preserve `terminalThreadId` only when issue is null | When user switches issues, console should follow. When user closes detail, console should stay where it was so output is still readable. |
| Move `TerminalDrawer` outside `OverlayPanel`'s relative container | `OverlayPanel` uses `absolute inset-y-0` which spans its parent. Putting the terminal as a sibling rather than inside the same parent is the minimal restructure that achieves Cursor-like behavior. |
| File post-hoc tracking issues for completed fixes | Pre-release repo, no PR review process yet. Issues serve as audit trail. |

### Files Changed

- `apps/desktop/src/main/index.ts` — before-quit awaits `killAllAndWait`, killInFlight guard
- `apps/desktop/src/renderer/App.tsx` — terminal moved outside overlay's relative container, full content width
- `apps/desktop/src/renderer/stores/app-store.ts` — selectIssue preserves terminalThreadId on null
- `apps/desktop/src/renderer/stores/app-store.test.ts` — new test for pin preservation
- `packages/agents/src/process-manager.ts` — auto-cleanup + `killAllAndWait`
- `packages/agents/src/process-manager.test.ts` — 7 tests (4 + 3 new)
- `packages/db/src/index.ts` — call `migrateV37`
- `packages/db/src/schema.ts` — `migrateV37` body (4 indexes)
- `packages/db/src/schema.test.ts` — 26 tests, V37 block

### Verification

- `bun run test src/process-manager.test.ts` → 7/7 pass
- `bun run test src/schema.test.ts` → 26/26 pass
- `bun run test src/renderer/stores/app-store.test.ts` → 12/12 pass
- `bun run test src/renderer/App.test.tsx` → 4/4 pass
- `bun run typecheck` (repo-wide) → 15/15 clean

### Mistakes and Fixes

| Mistake | Fix |
|---------|-----|
| First SIGKILL escalation test used `vi.advanceTimersByTime(60)` + `await Promise.resolve()` — only flushed one microtask, but Promise.race needed multiple ticks to settle. Saw 1 call instead of 2. | Switched to `await vi.advanceTimersByTimeAsync(60)` which fully drains microtasks under fake timers. |

### GitHub Issues Filed

- [#62](https://github.com/shipshitdev/shipcode/issues/62) — ProcessManager registry leak
- [#63](https://github.com/shipshitdev/shipcode/issues/63) — Missing FK indexes (V37)
- [#64](https://github.com/shipshitdev/shipcode/issues/64) — before-quit pty orphans

### Next Steps

- [ ] Manual QA: console full-width with detail open, resize behavior, maximize-terminal mode, expanded-issue mode
- [ ] Commit + push (waiting for explicit ask per CLAUDE.md auto-push rule)
- [ ] Optional follow-up: clear console pin on `selectProject` switch
- [ ] Optional follow-up: joint constraint between `issueDetailWidth` and terminal min content width

---

## Session 2 — Commit pending work + fix silent FAILED-state UI

**Status:** Complete

### Context

Resumed after `/clear` from a long 2026-04-27 session. Working tree had 11 modified + 2 untracked files spanning three topics:

1. Updater 404 handling (today's earlier work)
2. Pipeline executor error extraction + UI helpers fix (today's earlier work)
3. Project settings refresh-git-remote feature (pre-existing branch state)

User then sent screenshot of issue #56 in FAILED state showing only "Retry will resume from execution using the latest structured plan." with no error message. Root cause: many `emitPhase(threadId, 'failed')` calls in pipeline pass no message, leaving `thread.lastError` null. UI then hid the entire error block since it was gated on `(lastError || failingPhaseOutput)`.

### System Flow

```
working tree (11 M + 2 ??)
       │
       ▼
group commits by topic
       │
       ├── 1ebcf09 updater 404 → up-to-date
       ├── c3fe920 executor error snippet (no plan-JSON leak)
       └── f531cb2 project settings refresh git remote
       │
       ▼
inventory silent emitPhase('failed') sites
       │
       ├── execution-phases.ts (7 sites): verify start, empty diff,
       │       retries exhausted, parse fail, verify catch,
       │       head moved, no commits ahead, push retry, shipping catch
       └── planning-phases.ts (3 sites): missing structured plan,
               unknown decision, review parse fail, revision parse fail
       │
       ▼
patch each site → meaningful error string
       │
       ▼
update IssueDetailActions: always render danger card while canRerun;
       fallback hint when no info captured
       │
       ▼
fix 11 pipeline.test.ts assertions: undefined → expect.any(String)
       │
       ▼
8a165cb fix(pipeline+ui): always surface a reason for FAILED state
```

### Affected Components

- **Pipeline backend**: `packages/pipeline/src/pipeline/execution-phases.ts`, `packages/pipeline/src/pipeline/planning-phases.ts`
- **Pipeline tests**: `packages/pipeline/src/pipeline.test.ts` + new `execution-phases.test.ts`
- **Renderer UI**: `apps/desktop/src/renderer/components/issue-detail/IssueDetailActions.tsx`, `helpers.ts` + `helpers.test.ts`
- **Updater (main)**: `apps/desktop/src/main/update-service.ts` + new `update-service.test.ts`
- **Project settings**: `apps/desktop/src/main/ipc/register-project-handlers.ts`, `packages/db/src/queries/projects.ts`, `packages/shared/src/ipc-channels.ts`, `apps/desktop/src/renderer/App.tsx`, `ProjectSettingsModal.tsx`, `ProjectSettingsGeneralTab.tsx`, `ProjectSettingsLeafTabs.test.tsx`

### What Was Done

- [x] Audited working tree — categorized 13 files into 3 topics
- [x] Committed updater 404 fix (1ebcf09)
- [x] Committed executor error snippet + helpers executing-phase fix (c3fe920)
- [x] Committed project settings refresh git remote feature (f531cb2)
- [x] Inventoried all silent `emitPhase('failed')` calls — 10 distinct call sites across execution-phases (7) and planning-phases (3)
- [x] Patched every silent failure with a phase-specific error message
- [x] Replaced two swallowing `catch {}` blocks (verify, shipping) with named-binding catches that capture the underlying message
- [x] Updated `IssueDetailActions.tsx` rerunSection — always renders danger card when `canRerun`; italic fallback hint when neither `lastError` nor `failingPhaseOutput` exists
- [x] Fixed 11 pipeline.test.ts assertions: `undefined` → `expect.any(String)` for the new error arg on `recordFailure`
- [x] Verified scoped tests: pipeline 82/82, IssueDetail 35/35, helpers 17/17, exec-phases 6/6
- [x] Committed pipeline+UI fix (8a165cb)

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| Patch every silent `emitPhase('failed')` with a specific message | Empty `lastError` ⇒ hidden error block ⇒ user sees FAILED with no clue. Each phase already knows why it's failing — pass that string. |
| Always render danger card while `canRerun` is true | Visual consistency. Issues failed *before* this fix still have null `lastError`; show "Pipeline error" + fallback pointer to Pipeline-tab terminal output. |
| Capture `firstErr` + `secondErr` in commit-and-push double-catch | Push retry currently swallows both errors. Truncate each to 120 chars to diagnose remote/auth/lock issues. |
| Relax test assertion to `expect.any(String)` rather than hard-coding new strings | Tests assert *that* a failure was recorded, not the wording. Hard-coded strings would brittle every error wording change. |
| Three small commits + one fix commit, no force-push, no push at all | User said "just commit. release pipeline still failing too much." No tag/release/deploy commands run. |

### Files Changed

```
1ebcf09  apps/desktop/src/main/update-service.ts
1ebcf09  apps/desktop/src/main/update-service.test.ts                                    (new)

c3fe920  packages/pipeline/src/pipeline/execution-phases.ts
c3fe920  packages/pipeline/src/pipeline/execution-phases.test.ts                        (new)
c3fe920  apps/desktop/src/renderer/components/issue-detail/helpers.ts
c3fe920  apps/desktop/src/renderer/components/issue-detail/helpers.test.ts

f531cb2  apps/desktop/src/main/ipc/register-project-handlers.ts
f531cb2  apps/desktop/src/renderer/App.tsx
f531cb2  apps/desktop/src/renderer/components/ProjectSettingsModal.tsx
f531cb2  apps/desktop/src/renderer/components/project-settings-modal/ProjectSettingsGeneralTab.tsx
f531cb2  apps/desktop/src/renderer/components/project-settings-modal/ProjectSettingsLeafTabs.test.tsx
f531cb2  packages/db/src/queries/projects.ts
f531cb2  packages/shared/src/ipc-channels.ts

8a165cb  packages/pipeline/src/pipeline/execution-phases.ts                              (7 silent sites)
8a165cb  packages/pipeline/src/pipeline/planning-phases.ts                               (3 silent sites)
8a165cb  packages/pipeline/src/pipeline.test.ts                                          (11 assertion fixes)
8a165cb  apps/desktop/src/renderer/components/issue-detail/IssueDetailActions.tsx        (always render card)
```

### Verification

- `cd packages/pipeline && bun run test src/pipeline.test.ts` → 82/82 pass
- `cd packages/pipeline && bun run test src/pipeline/execution-phases.test.ts` → 6/6 pass
- `cd apps/desktop && bun run test src/renderer/components/IssueDetail` → 35/35 pass
- `cd apps/desktop && bun run test src/renderer/components/issue-detail/helpers.test.ts` → 17/17 pass

### Mistakes and Fixes

| Mistake | Fix |
|---------|-----|
| First scoped test run used `bun run test <file>` from repo root → turbo treats path as a task name (`Could not find task`). | Run `bun run test <relative-path>` from inside the package directory (`cd packages/pipeline && bun run test src/...`). Uses package vitest, not turbo. |

### Anchors / Patterns

- Rule: every `emitPhase(threadId, 'failed')` MUST carry a message string. Silent failure = user-blind FAILED state.
- `expect.any(String)` for `recordFailure` arg slot in pipeline tests (don't hard-code message wording).
- Danger card in `rerunSection` always renders while `canRerun`; conditional fallback hint when no captured info.

### Next Steps

- [ ] Existing FAILED issue #56 will still show fallback hint (no `lastError` ever captured). User can retry — fresh run will populate proper error.
- [ ] Release pipeline still flaky per user; not touched this session.
- [ ] No push performed — user said commit only.

---

## Session 3 — Resumed-context wrap-up (no new code)

**Status:** Complete (no working-tree changes)

### Context

Conversation auto-resumed after compaction with the EXECUTION FAILED bug investigation queued as the pending task. Re-traced same root cause already addressed in commits c3fe920 + 8a165cb (Session 2). Git status clean — implementation already on `develop` (24 ahead of origin).

### What Was Done

- [x] Re-investigated `resolveFailingPhaseOutput` fall-through to plan output for `executing` failures.
- [x] Re-investigated `extractExecutionErrorSnippet` weak `startsWith('{')` guard letting backtick-fenced plan blocks leak into `lastError`.
- [x] Confirmed both fixes already landed: `helpers.ts` has explicit `case 'executing': return null`; `execution-phases.ts` has the multi-strategy reverse-scan extractor with 6 unit tests.
- [x] Verified scoped tests pass: `bunx vitest run helpers.test.ts` → 17/17, `execution-phases.test.ts` → 6/6.
- [x] Typecheck clean across pipeline + desktop.

### Files Changed

None (working tree clean, all fixes already committed).

### Next Steps

- Same as Session 2: retry issue #56 in fresh run to verify proper `lastError` populates.
- 24 unpushed commits sitting on `develop` — user has not asked for push.

---

## Session 4 — Console layout v2: detail panel as row-level sibling (Cursor-style flanking)

**Status:** Complete. Layout change rode along with commit `f531cb2`. All 306 desktop tests green.

### Context

Vincent shared two new screenshots comparing ShipCode's console to Cursor's terminal. Session 1's earlier fix made the terminal full content-width (sibling of upper region), but the right detail panel was still nested inside the center column as an `absolute` overlay — terminal extended UNDER the detail panel horizontally. Vincent's ask: "the console in shipcode should be like in cursor. BETWEEN the sidebars. not above or below it. resize the console WHEN sidebars are open." → terminal must be flanked by both the left sidebar AND the right detail panel, resizing live as either flank toggles.

### System Flow

```
Session 1 layout (full-width terminal — still extended under detail overlay)
┌──────┬───────────────────────────────────┐
│ Side │ ┌────────┬───────────────┐        │
│ bar  │ │ views  │ IssueDetail   │        │
│      │ │        │ (absolute)    │        │
│      │ └────────┴───────────────┘        │
│      ├───────────────────────────────────┤
│      │ TerminalDrawer (full content w)   │ ← extends under detail
└──────┴───────────────────────────────────┘

Session 4 layout (detail panel = row sibling)
┌──────┬─────────────────────────┬─────────┐
│ Side │ views                   │ Issue   │
│ bar  ├─────────────────────────┤ Detail  │
│      │ TerminalDrawer          │ (col)   │
│      │ (between sidebar &      │         │
│      │  detail)                │         │
└──────┴─────────────────────────┴─────────┘
```

### Root Cause

`apps/desktop/src/renderer/App.tsx` rendered the right detail panel as an `absolute inset-y-0` `OverlayPanel` scoped to the upper region only. Even after Session 1 promoted the terminal to a sibling of the upper region (full content width), the detail panel stayed nested INSIDE the center column → terminal extended under it horizontally.

### Fix

Promote the detail panel out of the center column to row-level: `[sidebar | center | detail]` as flex siblings. Center column = main view + terminal. Right detail panel = sibling div with `data-slot="overlay-panel"` (matches existing test selector) styled as a docked column with `flex-shrink-0`, explicit `width` style, left-edge resize handle. `OverlayPanel` import dropped because the docked use-case wants normal flex flow, not absolute positioning.

### Affected Components

- `apps/desktop/src/renderer/App.tsx` — single-file restructure

### What Was Done

- [x] Read `App.tsx` Session 1 layout block
- [x] Read `OverlayPanel` (`packages/ui/src/primitives/overlay-panel.tsx`) — confirmed `absolute inset-y-0` makes it unsuitable as a docked column
- [x] Read `TerminalDrawer.tsx` — confirmed it's a `flex flex-col shrink-0` block whose width follows parent, so promoting parent works automatically
- [x] Restructured `App.tsx` row to `[sidebar | center | detail]`
- [x] Inlined the docked detail-panel div with `data-slot="overlay-panel"` plus matching classes (`pointer-events-auto relative overflow-hidden`) so existing `App.test.tsx` selector keeps matching
- [x] Inlined the resize handle on the panel's left edge (`w-1 -translate-x-1/2 cursor-col-resize`)
- [x] Dropped `import { OverlayPanel } from '@shipshitdev/ui'`
- [x] Expanded `IssueDetail` (takeover mode) renders inside center column above terminal — terminal still pinned to bottom
- [x] `bun run typecheck` clean
- [x] `bun run test` → 306/306 pass

### Key Decisions

| Decision | Rationale |
|----------|-----------|
| Promote detail panel to row-level sibling | Single flex container `[sidebar \| center \| detail]` gives correct widths automatically when any flank toggles — flex layout handles resize live, no observers or width math |
| Inline the docked panel, don't reuse `OverlayPanel` | `OverlayPanel` is `absolute inset-y-0` by design (overlay use-case); a docked column wants normal flex flow. Inlining keeps `OverlayPanel` semantics intact for other consumers |
| Keep `data-slot="overlay-panel"` + class set on the inlined div | `App.test.tsx` queries by that slot and asserts `pointer-events-auto relative overflow-hidden` + `width: 480px` — preserving the contract avoids test churn |
| Render expanded `IssueDetail` inside the center column | Expanded mode is takeover; terminal still pinned to bottom of center column. There's no docked detail panel in expanded mode by definition |
| Leave `terminalMaximized` behavior unchanged | Maximize hides the upper region only; detail panel still flanks horizontally. If user wants horizontal fullscreen too they can collapse the panel |

### Files Changed

- `apps/desktop/src/renderer/App.tsx` — restructured layout block, dropped `OverlayPanel` import, inlined docked detail panel with resize handle (rode along with commit `f531cb2`)

### Mistakes and Fixes

- **Mistake:** First instinct: keep using `OverlayPanel` inside a sized wrapper div. Would have introduced double-width bookkeeping (wrapper width + inner panel width both tied to `issueDetailWidth`).
  - **Fix:** Inlined the panel with the same `data-slot` + classes — single source of truth for width, OverlayPanel stays untouched for other call sites.

### Verification

- `cd apps/desktop && bun run typecheck` clean
- `cd apps/desktop && bun run test` → 57 files / 306 tests pass
- `App.test.tsx` overlay-panel test still resolves the docked panel by `[data-slot="overlay-panel"]` and asserts `pointer-events-auto relative overflow-hidden` + `width: 480px`

### Result

Console now flanked by sidebar (left) and detail panel (right). Both flanks resize the terminal width live via flex layout. Matches Cursor's terminal behavior.

### Next Steps

- [ ] Smoke-test: open issue with detail panel visible, toggle terminal, drag-resize the detail panel, verify terminal width tracks live
- [ ] Smoke-test: maximized terminal with detail panel open — confirm right column still visible (current trade)
- [ ] If maximize should hide detail panel too, add `&& !terminalMaximized` to the panel render guard

---

## Session 5 — Project settings: refresh git remote + default branch dropdown (origin session for `f531cb2`)

**Status:** Shipped (commit `f531cb2` — same one referenced by Session 2's commit-grouping pass).

> Session 2 above bundled this work into a multi-topic commit pass and listed the file changes, but did not capture the design decisions made during the original implementation turn. This entry records those decisions so the rationale isn't lost.

### Context

User opened Project Settings on a renamed GitHub repo (`Gateway-Ventures/vitay-remastered` → `Gateway-Ventures/veref.work.git` on disk per `git remote -v`). Screenshot raised two complaints:

1. Renamed the repo on GitHub but ShipCode's `Git remote` row still showed the stale URL with no in-app way to fix it.
2. The `Default branch` row was static text, even though the Kanban toolbar already has a Local/Remote dropdown for the same field.

My first answer suggested running `git remote set-url origin …` on disk. User correctly pushed back: *"if we save the value, we could have a button to refresh it."* That reframing drove the design.

### System Flow

```text
Project Settings opens
    │
    ▼
useQuery ['git-branches', projectId] (fetch:false) → cached branch list
    │
    ▼
General tab renders:
  Git remote   [stale URL]                  [RefreshCw icon] ──► refreshGitRemoteMutation
  Default br.  [Select Local|Remote groups] [RefreshCw icon] ──► setDefaultBranchMutation
                                                              │  refreshBranchesMutation
                                                              ▼
                                                       IPC handlers
    │                                               (project:refresh-git-remote NEW,
    │                                                project:set-default-branch reuse,
    │                                                git:list-branches reuse)
    ▼
GitService.getRemoteUrl()  ──►  ProjectQueries.updateGitRemote()
                                       │
                                       ▼
                            invalidateQueries(['project',id], ['thread-panel-data',id])
                                       │
                                       ▼
                                fresh remote + selected branch in UI
```

### Affected Components

- **Frontend (renderer):** `ProjectSettingsModal.tsx` (added `git-branches` query + 3 mutations); `ProjectSettingsGeneralTab.tsx` (new props, RefreshCw button next to remote, `Select` for default branch); `ProjectSettingsLeafTabs.test.tsx` (prop fixtures).
- **Backend (main):** `register-project-handlers.ts` (new `project:refresh-git-remote` handler reads via `GitService.getRemoteUrl()`).
- **Data:** `packages/db/src/queries/projects.ts` (new `updateGitRemote`); `packages/shared/src/ipc-channels.ts` (new channel).

### What Was Done

- [x] DB: `ProjectQueries.updateGitRemote(id, remote)` with `ISO_NOW_SQL` touch.
- [x] IPC channel + handler: `project:refresh-git-remote` re-reads origin via `GitService.getRemoteUrl`, persists when changed, returns `{ project, remote, changed }`.
- [x] Modal: `git-branches` query (`fetch:false`) + 3 mutations (`refreshGitRemoteMutation`, `setDefaultBranchMutation`, `refreshBranchesMutation`).
- [x] General tab: `Git remote` row → text + `RefreshCw` icon button (spins while pending). `Default branch` row → `Select` w/ Local/Remote `SelectGroup`s + refresh icon. Falls back to plain text + load button when branches list empty.
- [x] Tests: backfilled 7 new props in both `ProjectSettingsLeafTabs.test.tsx` render fixtures.

### Key Decisions (specific to this session)

| Decision | Rationale |
|----------|-----------|
| `project:refresh-git-remote` returns `{ project, remote, changed }` not just `Project` | `changed` lets callers later surface "no drift" feedback or auto-detect-on-open without an extra read. Forward-compatible. |
| Default branch saves immediately on `Select` change (no modal Save button needed) | Mirrors Kanban toolbar UX (`onBaseBranchChange` fires `project:set-default-branch` directly). Folding into modal save would duplicate validation and fight optimistic cache update. |
| Reuse existing `project:set-default-branch` IPC instead of inventing new save path | Already validates branch exists in `git:list-branches`. |
| `git-branches` query in modal uses `fetch:false` | Opening Settings shouldn't trigger a network `git fetch`. User can hit refresh icon explicitly — same pattern Kanban toolbar uses on initial render. |
| `RefreshCw` imported from `@shipshitdev/ui` (re-export), not direct `lucide-react` | App package has no direct lucide dep; matches existing `ThreadPanel.tsx` and `Titlebar.tsx`. |
| Icon button (no text label) for both refresh actions | Matches `BoardToolbar` icon button next to the Kanban dropdown. Tooltip carries the verb. |
| Fall back to plain text + load button when branches list empty | First open of Settings before any `git:list-branches` call → no dropdown to populate. Cheaper than auto-fetching on render. |

### Files Changed (commit `f531cb2`)

```
apps/desktop/src/main/ipc/register-project-handlers.ts                                      | +18
apps/desktop/src/renderer/components/ProjectSettingsModal.tsx                                | +74
apps/desktop/src/renderer/components/project-settings-modal/ProjectSettingsGeneralTab.tsx   | +109/-4
apps/desktop/src/renderer/components/project-settings-modal/ProjectSettingsLeafTabs.test.tsx| +14
packages/db/src/queries/projects.ts                                                         | +6
packages/shared/src/ipc-channels.ts                                                         | +4
apps/desktop/src/renderer/App.tsx                                                           | formatter pass (unrelated)
```

### Verification (this session, pre-commit)

- `bun run --filter @shipcode/desktop typecheck` → clean
- `bun run test src/renderer/components/project-settings-modal/ProjectSettingsLeafTabs.test.tsx` → 11/11
- `bun run test src/renderer/components/ProjectSettingsModal.test.tsx` → 4/4

### Mistakes and Fixes

| Mistake | Fix |
|---------|-----|
| First suggested user run `git remote set-url` on disk instead of building the in-app fix. | User pushed back; switched to the right answer. Lesson: when a fix could be local-shell or in-app, prefer in-app unless there's a specific reason. |
| Initial import `from 'lucide-react'` failed — not a direct dep of `apps/desktop`. | Imported `RefreshCw` from `@shipshitdev/ui`'s re-export, matching existing usage. |
| `bun run --filter @shipcode/desktop typecheck` complained `updateGitRemote` missing on `ProjectQueries` after the source edit landed. | Desktop typecheck reads `packages/db/dist`; ran `bun run --filter @shipcode/db build` to refresh dist. Cleared. |

### Anchors / Patterns

- Two refresh icons in Project Settings General tab now: remote (re-reads `git remote get-url origin`) and branches (re-runs `git:list-branches` with `fetch:true`).
- Default branch persists on `Select` change, not on modal Save — matches Kanban toolbar.
- `{ changed }` flag pattern on "refresh-from-source" IPC returns keeps drift signals cheap and forward-compatible.

### Next Steps

- [ ] Behavioural tests for the new tab actions (assert mutation handlers fire, `Select` shows Local/Remote groups when populated).
- [ ] Auto-detect drift on modal open: compare `await GitService.getRemoteUrl()` to `project.gitRemote`; render a yellow "Update?" badge if they differ.
- [ ] Surface `changed: false` from refresh as a small "Already up to date" toast.
- [ ] Optional: derive display name from `parseGithubRemote(...).repo` via a "Detect from origin" button on the Name field.
