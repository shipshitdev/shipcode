{
  "envFiles": [],
  "setupBeforeVerify": false,
  "setupCommands": [
    "bun install"
  ],
  "testingContext": "This is a Turborepo + Bun monorepo. Use Vitest for package tests. Prefer targeted tests for touched packages when possible, but the repo-level verification contract is `bun run typecheck` followed by `bun run test`.",
  "verifyCommands": [
    "bun run typecheck",
    "bun run test"
  ],
  "version": 1
}
